// Generated by redwood. Do not edit by hand.
import { snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
export class Workspaces {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List workspaces
     *
     * @example
     * ```ts
     * const page = await client.workspaces.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces`, query: { limit: params?.limit, cursor: params?.cursor, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
}
//# sourceMappingURL=workspaces.js.map