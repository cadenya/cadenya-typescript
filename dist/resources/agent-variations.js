// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
import { wireAddAgentVariationAssignmentRequest } from '../types.js';
export class AgentVariations {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List variations
     *
     * @example
     * ```ts
     * const page = await client.agents.variations.list('agent_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(agentId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations`, query: { limit: params?.limit, cursor: params?.cursor, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list(agentId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Create a new variation
     *
     * @example
     * ```ts
     * const agentVariation = await client.agents.variations.create('agent_123', { metadata: { name: 'sample' }, spec: {  } });
     * ```
     */
    create(agentId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations`, body: { metadata: params.metadata, spec: params.spec } };
        }, options);
    }
    /**
     * Get a variation by ID
     *
     * @example
     * ```ts
     * const agentVariation = await client.agents.variations.retrieve('agent_123', '_123');
     * ```
     */
    retrieve(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Delete a variation
     *
     * @example
     * ```ts
     * await client.agents.variations.delete('agent_123', '_123');
     * ```
     */
    delete(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Update a variation
     *
     * @example
     * ```ts
     * const agentVariation = await client.agents.variations.update('agent_123', '_123');
     * ```
     */
    update(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'PATCH', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('id', id)}`, body: { metadata: params?.metadata, spec: params?.spec, updateMask: params?.updateMask } };
        }, options);
    }
    /**
     * Add an assignment to a variation
     *
     * @example
     * ```ts
     * const variationAssignment = await client.agents.variations.addAssignment('agent_123', 'variation_123', { body: { toolId: 'sample', type: 'toolId' } });
     * ```
     */
    addAssignment(agentId, variationId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('variationId', variationId)}/assignments`, body: wireAddAgentVariationAssignmentRequest(params.body) };
        }, options);
    }
    /**
     * Remove an assignment from a variation
     *
     * @example
     * ```ts
     * await client.agents.variations.removeAssignment('agent_123', 'variation_123', '_123');
     * ```
     */
    removeAssignment(agentId, variationId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('variationId', variationId)}/assignments/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Attach a memory layer to a variation
     *
     * @example
     * ```ts
     * const variationMemoryLayerAssignment = await client.agents.variations.addMemoryLayer('agent_123', 'variation_123', { memoryLayerId: 'sample' });
     * ```
     */
    addMemoryLayer(agentId, variationId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('variationId', variationId)}/memory_layer_assignments`, body: { memoryLayerId: params.memoryLayerId, position: params.position } };
        }, options);
    }
    /**
     * Remove a memory layer assignment from a variation
     *
     * @example
     * ```ts
     * await client.agents.variations.removeMemoryLayer('agent_123', 'variation_123', '_123');
     * ```
     */
    removeMemoryLayer(agentId, variationId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('variationId', variationId)}/memory_layer_assignments/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Update a variation's memory layer assignment
     *
     * @example
     * ```ts
     * const variationMemoryLayerAssignment = await client.agents.variations.updateMemoryLayer('agent_123', 'variation_123', '_123');
     * ```
     */
    updateMemoryLayer(agentId, variationId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'PATCH', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/variations/${pathSegment('variationId', variationId)}/memory_layer_assignments/${pathSegment('id', id)}`, body: { position: params?.position } };
        }, options);
    }
}
//# sourceMappingURL=agent-variations.js.map