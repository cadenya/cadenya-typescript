// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
export class Models {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List models
     *
     * @example
     * ```ts
     * const page = await client.models.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/models`, query: { limit: params?.limit, cursor: params?.cursor, prefix: params?.prefix, query: params?.query, state: params?.state, aiProviderKeyId: params?.aiProviderKeyId, isAssigned: params?.isAssigned, labels: params?.labels, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
    /**
     * Get a model by ID
     *
     * @example
     * ```ts
     * const model = await client.models.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/models/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Disable a model
     *
     * @example
     * ```ts
     * const model = await client.models.disable('_123');
     * ```
     */
    disable(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/models/${pathSegment('id', id)}:disable` };
        }, options);
    }
    /**
     * Enable a model
     *
     * @example
     * ```ts
     * const model = await client.models.enable('_123');
     * ```
     */
    enable(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/models/${pathSegment('id', id)}:enable` };
        }, options);
    }
    /**
     * Swap models on agent variations
     *
     * @example
     * ```ts
     * await client.models.swapOnVariations();
     * ```
     */
    swapOnVariations(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/models:swapModelOnVariations`, body: { modelSwaps: params?.modelSwaps }, void: true };
        }, options);
    }
}
//# sourceMappingURL=models.js.map