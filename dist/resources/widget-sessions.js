// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
import { wireWidgetSessionSpec } from '../types.js';
export class WidgetSessions {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List widget sessions
     *
     * @example
     * ```ts
     * const page = await client.widgetSessions.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions`, query: { limit: params?.limit, cursor: params?.cursor, widgetId: params?.widgetId, tenantId: params?.tenantId, subjectId: params?.subjectId, state: params?.state, labels: params?.labels, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
    /**
     * Create a widget session
     *
     * @example
     * ```ts
     * const widgetSession = await client.widgetSessions.create({ spec: { widgetId: 'sample' } });
     * ```
     */
    create(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions`, body: { metadata: params.metadata, spec: wireWidgetSessionSpec(params.spec), secrets: params.secrets } };
        }, options);
    }
    /**
     * Delete all of a tenant's widget sessions
     *
     * @example
     * ```ts
     * const deleteTenantWidgetSessionsResponse = await client.widgetSessions.deleteTenant();
     * ```
     */
    deleteTenant(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions`, query: { tenantId: params?.tenantId } };
        }, options);
    }
    /**
     * Get a widget session by ID
     *
     * @example
     * ```ts
     * const widgetSession = await client.widgetSessions.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Delete a widget session
     *
     * @example
     * ```ts
     * await client.widgetSessions.delete('_123');
     * ```
     */
    delete(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Revoke a widget session
     *
     * @example
     * ```ts
     * const widgetSession = await client.widgetSessions.revoke('_123');
     * ```
     */
    revoke(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/widget_sessions/${pathSegment('id', id)}:revoke` };
        }, options);
    }
}
//# sourceMappingURL=widget-sessions.js.map