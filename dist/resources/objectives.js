// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
import { Stream } from '../core/sse.js';
import { wireObjectiveEpisodicConfig } from '../types.js';
export class Objectives {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List objectives
     *
     * @example
     * ```ts
     * const page = await client.objectives.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives`, query: { limit: params?.limit, cursor: params?.cursor, agentId: params?.agentId, parentObjectiveId: params?.parentObjectiveId, state: params?.state, profileId: params?.profileId, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo, agentScheduleId: params?.agentScheduleId, labels: params?.labels, tenantId: params?.tenantId, subjectId: params?.subjectId, widgetId: params?.widgetId, widgetSessionId: params?.widgetSessionId } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
    /**
     * Create a new objective
     *
     * @example
     * ```ts
     * const objective = await client.objectives.create({ agentId: 'sample', systemPromptData: {  } });
     * ```
     */
    create(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives`, body: { agentId: params.agentId, variationId: params.variationId, metadata: params.metadata, systemPromptData: params.systemPromptData, firstUserMessage: params.firstUserMessage, secrets: params.secrets, memoryCascade: params.memoryCascade, firstUserMessageData: params.firstUserMessageData, episodicMemory: wireObjectiveEpisodicConfig(params.episodicMemory), tenant: params.tenant, subject: params.subject, pinnedParameters: params.pinnedParameters } };
        }, options);
    }
    /**
     * Get an objective by ID
     *
     * @example
     * ```ts
     * const objective = await client.objectives.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * List objective context windows
     *
     * @example
     * ```ts
     * const page = await client.objectives.listContextWindows('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listContextWindows(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/context_windows`, query: { limit: params?.limit, cursor: params?.cursor, includeInfo: params?.includeInfo, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listContextWindows(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Get objective context diagnostics
     *
     * @example
     * ```ts
     * const getObjectiveDiagnosticsResponse = await client.objectives.retrieveDiagnostics('objective_123');
     * ```
     */
    retrieveDiagnostics(objectiveId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/diagnostics` };
        }, options);
    }
    /**
     * List objective events
     *
     * @example
     * ```ts
     * const page = await client.objectives.listEvents('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listEvents(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/events`, query: { limit: params?.limit, cursor: params?.cursor, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo, windowId: params?.windowId, sinceEventId: params?.sinceEventId, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listEvents(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Stream objective events
     *
     * @example
     * ```ts
     * const stream = await client.objectives.streamEvents('objective_123');
     * for await (const event of stream) {
     *   // typed event payloads; housekeeping frames are skipped
     * }
     * ```
     */
    async streamEvents(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _spec = { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/events:stream`, stream: true };
        const response = await this._client.rawRequest(_spec, options);
        return new Stream(response, options?.signal, options?.lastEventId, ['ping', 'open'], options?.reconnect === false ? undefined : (lastEventId, signal) => this._client.rawRequest(_spec, { ...options, lastEventId, signal }));
    }
    /**
     * List feedback for an objective
     *
     * @example
     * ```ts
     * const page = await client.objectives.listFeedback('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listFeedback(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/feedback`, query: { limit: params?.limit, cursor: params?.cursor, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listFeedback(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Submit feedback for an objective
     *
     * @example
     * ```ts
     * const objectiveFeedback = await client.objectives.createFeedback('objective_123', { metadata: {  }, data: {  } });
     * ```
     */
    createFeedback(objectiveId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/feedback`, body: { metadata: params.metadata, data: params.data } };
        }, options);
    }
    /**
     * List objective tasks
     *
     * @example
     * ```ts
     * const page = await client.objectives.listTasks('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listTasks(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tasks`, query: { limit: params?.limit, cursor: params?.cursor, sortOrder: params?.sortOrder } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listTasks(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Get an objective task by ID
     *
     * @example
     * ```ts
     * const objectiveTask = await client.objectives.retrieveTask('objective_123', '_123');
     * ```
     */
    retrieveTask(objectiveId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tasks/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * List objective tool calls
     *
     * @example
     * ```ts
     * const page = await client.objectives.listToolCalls('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listToolCalls(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tool_calls`, query: { limit: params?.limit, cursor: params?.cursor, status: params?.status, includeInfo: params?.includeInfo, executionStatus: params?.executionStatus, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listToolCalls(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Get an objective tool call by ID
     *
     * @example
     * ```ts
     * const objectiveToolCallWithResult = await client.objectives.retrieveToolCall('objective_123', 'tool_call_123');
     * ```
     */
    retrieveToolCall(objectiveId, toolCallId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tool_calls/${pathSegment('toolCallId', toolCallId)}` };
        }, options);
    }
    /**
     * Approve a tool call
     *
     * @example
     * ```ts
     * const objectiveToolCall = await client.objectives.approveToolCall('objective_123', 'tool_call_123');
     * ```
     */
    approveToolCall(objectiveId, toolCallId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tool_calls/${pathSegment('toolCallId', toolCallId)}:approve` };
        }, options);
    }
    /**
     * Deny a tool call
     *
     * @example
     * ```ts
     * const objectiveToolCall = await client.objectives.denyToolCall('objective_123', 'tool_call_123');
     * ```
     */
    denyToolCall(objectiveId, toolCallId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tool_calls/${pathSegment('toolCallId', toolCallId)}:deny`, body: { memo: params?.memo } };
        }, options);
    }
    /**
     * Set a bare tool call's content
     *
     * @example
     * ```ts
     * const objectiveToolCall = await client.objectives.setToolCallContent('objective_123', 'tool_call_123', { content: [{ text: { text: 'sample' }, type: 'text' }] });
     * ```
     */
    setToolCallContent(objectiveId, toolCallId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tool_calls/${pathSegment('toolCallId', toolCallId)}:setContent`, body: { content: params.content } };
        }, options);
    }
    /**
     * List objective tools
     *
     * @example
     * ```ts
     * const page = await client.objectives.listTools('objective_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listTools(objectiveId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}/tools`, query: { limit: params?.limit, cursor: params?.cursor } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listTools(objectiveId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Cancel an objective
     *
     * @example
     * ```ts
     * const objective = await client.objectives.cancel('objective_123');
     * ```
     */
    cancel(objectiveId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}:cancel`, body: { reason: params?.reason } };
        }, options);
    }
    /**
     * Compact an objective
     *
     * @example
     * ```ts
     * const compactObjectiveResponse = await client.objectives.compact('objective_123');
     * ```
     */
    compact(objectiveId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}:compact`, body: { compactionConfig: params?.compactionConfig } };
        }, options);
    }
    /**
     * Continue an objective
     *
     * @example
     * ```ts
     * const objectiveEvent = await client.objectives.continue('objective_123', { message: 'sample' });
     * ```
     */
    continue(objectiveId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/objectives/${pathSegment('objectiveId', objectiveId)}:continue`, body: { message: params.message, enqueue: params.enqueue } };
        }, options);
    }
}
//# sourceMappingURL=objectives.js.map