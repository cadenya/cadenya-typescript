// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
import { MemoryEntries } from './memory-entries.js';
import { wireMemoryLayerSpec } from '../types.js';
export class MemoryLayers {
    _client;
    entries;
    constructor(_client) {
        this._client = _client;
        this.entries = new MemoryEntries(_client);
    }
    /**
     * List memory layers
     *
     * @example
     * ```ts
     * const page = await client.memoryLayers.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/memory_layers`, query: { limit: params?.limit, cursor: params?.cursor, prefix: params?.prefix, query: params?.query, type: params?.type, agentId: params?.agentId, episodicKeyPrefix: params?.episodicKeyPrefix, labels: params?.labels, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
    /**
     * Create a new memory layer
     *
     * @example
     * ```ts
     * const memoryLayer = await client.memoryLayers.create({ metadata: { name: 'sample' }, spec: { type: 'MEMORY_LAYER_TYPE_EPISODIC' } });
     * ```
     */
    create(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/memory_layers`, body: { metadata: params.metadata, spec: wireMemoryLayerSpec(params.spec) } };
        }, options);
    }
    /**
     * Get a memory layer by ID
     *
     * @example
     * ```ts
     * const memoryLayer = await client.memoryLayers.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/memory_layers/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Delete a memory layer
     *
     * @example
     * ```ts
     * await client.memoryLayers.delete('_123');
     * ```
     */
    delete(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/memory_layers/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Update a memory layer
     *
     * @example
     * ```ts
     * const memoryLayer = await client.memoryLayers.update('_123');
     * ```
     */
    update(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'PATCH', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/memory_layers/${pathSegment('id', id)}`, body: { metadata: params?.metadata, spec: wireMemoryLayerSpec(params?.spec), updateMask: params?.updateMask } };
        }, options);
    }
}
//# sourceMappingURL=memory-layers.js.map