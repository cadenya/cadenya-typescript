// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
import { AgentSchedules } from './agent-schedules.js';
import { AgentVariations } from './agent-variations.js';
import { wireCreateAgentVariationRequest } from '../types.js';
export class Agents {
    _client;
    schedules;
    variations;
    constructor(_client) {
        this._client = _client;
        this.schedules = new AgentSchedules(_client);
        this.variations = new AgentVariations(_client);
    }
    /**
     * List agents
     *
     * @example
     * ```ts
     * const page = await client.agents.list();
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents`, query: { limit: params?.limit, cursor: params?.cursor, prefix: params?.prefix, query: params?.query, state: params?.state, variationSelectionMode: params?.variationSelectionMode, labels: params?.labels, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list({ ..._base, cursor: cursor }, options));
    }
    /**
     * Create a new agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.create({ metadata: { name: 'sample' }, spec: { variationSelectionMode: 'VARIATION_SELECTION_MODE_UNSPECIFIED' } });
     * ```
     */
    create(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents`, body: { metadata: params.metadata, spec: params.spec, defaultVariation: wireCreateAgentVariationRequest(params.defaultVariation) } };
        }, options);
    }
    /**
     * List feedback for an agent
     *
     * @example
     * ```ts
     * const page = await client.agents.listFeedback('agent_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listFeedback(agentId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/feedback`, query: { limit: params?.limit, cursor: params?.cursor, query: params?.query, sentiment: params?.sentiment, agentVariationId: params?.agentVariationId, createdAfter: params?.createdAfter, createdBefore: params?.createdBefore, labels: params?.labels, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listFeedback(agentId, { ..._base, cursor: cursor }, options));
    }
    /**
     * List webhook deliveries
     *
     * @example
     * ```ts
     * const page = await client.agents.listWebhookDeliveries('agent_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async listWebhookDeliveries(agentId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/webhook_deliveries`, query: { cursor: params?.cursor, limit: params?.limit, objectiveId: params?.objectiveId, eventType: params?.eventType, labels: params?.labels } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.listWebhookDeliveries(agentId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Get an agent by ID
     *
     * @example
     * ```ts
     * const agent = await client.agents.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Delete an agent
     *
     * @example
     * ```ts
     * await client.agents.delete('_123');
     * ```
     */
    delete(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Update an agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.update('_123');
     * ```
     */
    update(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'PATCH', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}`, body: { metadata: params?.metadata, spec: params?.spec, updateMask: params?.updateMask } };
        }, options);
    }
    /**
     * Archive an agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.archive('_123');
     * ```
     */
    archive(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}:archive` };
        }, options);
    }
    /**
     * Publish an agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.publish('_123');
     * ```
     */
    publish(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}:publish` };
        }, options);
    }
    /**
     * Unarchive an agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.unarchive('_123');
     * ```
     */
    unarchive(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}:unarchive` };
        }, options);
    }
    /**
     * Unpublish an agent
     *
     * @example
     * ```ts
     * const agent = await client.agents.unpublish('_123');
     * ```
     */
    unpublish(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('id', id)}:unpublish` };
        }, options);
    }
}
//# sourceMappingURL=agents.js.map