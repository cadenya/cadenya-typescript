// Generated by redwood. Do not edit by hand.
import { pathSegment } from '../core/http.js';
export class Uploads {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * Create an upload
     *
     * @example
     * ```ts
     * const upload = await client.uploads.create({ metadata: { name: 'sample' }, spec: { contentType: 'sample', filename: 'sample', sizeBytes: 'sample' } });
     * ```
     */
    create(params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/uploads`, body: { metadata: params.metadata, spec: params.spec } };
        }, options);
    }
    /**
     * Get an upload by ID
     *
     * @example
     * ```ts
     * const upload = await client.uploads.retrieve('_123');
     * ```
     */
    retrieve(id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/uploads/${pathSegment('id', id)}` };
        }, options);
    }
}
//# sourceMappingURL=uploads.js.map