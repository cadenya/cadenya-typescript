// Generated by redwood. Do not edit by hand.
import { pathSegment, snapshotParams } from '../core/http.js';
import { Page } from '../core/pagination.js';
export class AgentSchedules {
    _client;
    constructor(_client) {
        this._client = _client;
    }
    /**
     * List schedules
     *
     * @example
     * ```ts
     * const page = await client.agents.schedules.list('agent_123');
     * for await (const item of page) {
     *   // auto-fetches every page
     * }
     * ```
     */
    async list(agentId, params, options) {
        const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
        if (workspaceId === undefined)
            throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
        const _base = snapshotParams(params);
        const response = await this._client.request({ method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules`, query: { limit: params?.limit, cursor: params?.cursor, prefix: params?.prefix, query: params?.query, labels: params?.labels, sortOrder: params?.sortOrder, includeInfo: params?.includeInfo } }, options);
        return new Page(response.items ?? [], response.pagination?.nextCursor, (cursor) => this.list(agentId, { ..._base, cursor: cursor }, options));
    }
    /**
     * Create a new schedule
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.create('agent_123', { metadata: { name: 'sample' }, spec: { schedule: {  } } });
     * ```
     */
    create(agentId, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules`, body: { metadata: params.metadata, spec: params.spec } };
        }, options);
    }
    /**
     * Get a schedule by ID
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.retrieve('agent_123', '_123');
     * ```
     */
    retrieve(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'GET', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}` };
        }, options);
    }
    /**
     * Delete a schedule
     *
     * @example
     * ```ts
     * await client.agents.schedules.delete('agent_123', '_123');
     * ```
     */
    delete(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'DELETE', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}`, void: true };
        }, options);
    }
    /**
     * Update a schedule
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.update('agent_123', '_123');
     * ```
     */
    update(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'PATCH', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}`, body: { metadata: params?.metadata, spec: params?.spec, updateMask: params?.updateMask } };
        }, options);
    }
    /**
     * Archive a schedule
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.archive('agent_123', '_123');
     * ```
     */
    archive(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}:archive` };
        }, options);
    }
    /**
     * Pause a schedule
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.pause('agent_123', '_123');
     * ```
     */
    pause(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}:pause` };
        }, options);
    }
    /**
     * Resume a schedule
     *
     * @example
     * ```ts
     * const agentSchedule = await client.agents.schedules.resume('agent_123', '_123');
     * ```
     */
    resume(agentId, id, params, options) {
        return this._client.requestAPI(() => {
            const workspaceId = String(params?.workspaceId ?? this._client.defaults['workspaceId'] ?? '').trim() || undefined;
            if (workspaceId === undefined)
                throw new Error("Missing 'workspaceId': pass it in params, set it on the client, or set the CADENYA_WORKSPACE_ID environment variable.");
            return { method: 'POST', path: `/v1/workspaces/${pathSegment('workspaceId', workspaceId)}/agents/${pathSegment('agentId', agentId)}/schedules/${pathSegment('id', id)}:resume` };
        }, options);
    }
}
//# sourceMappingURL=agent-schedules.js.map